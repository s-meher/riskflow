# RiskFlow dashboard

Internal ops UI for the RiskFlow API (React + Vite + TypeScript).

**Setup and run instructions are in the repository root:** [`../README.md`](../README.md).

```bash
cd dashboard
npm install
npm run dev
```

The dev server proxies `/api` to `http://localhost:8080` (see `vite.config.ts`).
