# Screenshots

These images are embedded from the repository root `README.md`.

| File | Description |
|------|-------------|
| `metrics.png` | Metrics row — operational snapshot |
| `events-form.png` | Submit event form + sample payload shortcuts |
| `latest-events.png` | Latest events table |
| `transactions.png` | Transactions list + search / status filter |
| `flagged-queue.png` | Manual review (flagged) queue |

To refresh screenshots, export PNGs at ~1200px wide and replace the files above (keep the same filenames so README links stay stable).
